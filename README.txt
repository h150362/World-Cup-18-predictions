
Viktig!
For at denne neste datavisualiseringen skal vises må plotly være innstalert ----> ($ pip install plotly) <----
(instalerses via pip)
i tilleg må ploty versionen være over: 1.9.4+ 
Test version med: 

import plotly

plotly.__version__

output: 'Din-versjon-her'

Oppdater plotly med:
pip install plotly --upgrade
